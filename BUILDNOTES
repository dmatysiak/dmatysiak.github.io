To rebuild your forest:

  forester build forest.toml

To create a new tree, add it to the `trees` directory, or invoke the following command for
forester to do it for you:

  forester new forest.toml --dest=trees